import { OnDomEnterDirective } from './on-dom-enter.directive';

describe('OnDomEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new OnDomEnterDirective();
    expect(directive).toBeTruthy();
  });
});
